# Lido automaticamente pelo `vite build` (mode=production) atraves de loadEnv.
# Em Firebase Hosting o app e servido na raiz do dominio, entao paths bundled
# precisam ser absolutos a partir de "/".
# NUNCA colocar segredos aqui — qualquer var VITE_* vira publica no bundle.
VITE_BASE_PATH=/
